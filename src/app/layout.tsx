import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Meher Cab Services — Fleet Management ERP",
  description: "Enterprise fleet management dashboard for Meher Cab Services. Manage drivers, vehicles, trips, and analytics in one place.",
  keywords: ["fleet management", "cab services", "ERP", "dashboard"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="antialiased">
        {children}
      </body>
    </html>
  );
}
